[pytest]
addopts = -ra
testpaths = tests
markers =
    unit: Focused unit-level validation checks.
    docs: Documentation and requirements contract checks.
    architecture: Architecture and service boundary checks.
