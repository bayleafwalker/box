# Knowledge Outputs

This directory holds committed outputs rendered from `kctl`.

- Canonical committed render path: `docs/knowledge/knowledge-base.md`
- Render command: `kctl render --output docs/knowledge/knowledge-base.md`
- Keep ad hoc renders and draft markdown out of the repo.

The rendered knowledge base is a publish artifact derived from approved and published entries in the local `kctl` database.
