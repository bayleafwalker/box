# Scenario Deployments

Scenario-specific overlays should land here once the simulator and service charts exist.

The intended model is:

- base platform services reconcile first
- service implementations reconcile next
- scenario overlays enable or pin the scenario-specific configuration last
