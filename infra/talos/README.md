# Talos Configuration

`cluster.yaml.example` captures the starting values expected by the current Hetzner/Talos bootstrap path.

Once the cluster design settles, this directory should hold:

- machine configuration inputs
- patch files for node roles
- upgrade notes pinned to the deployed Talos and Kubernetes versions
