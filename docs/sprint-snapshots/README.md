# Sprint Snapshots

This directory holds committed `sprintctl render` outputs.

- `sprint-current.txt` is the active execution snapshot.
- `backlog-master.txt` is the staged backlog snapshot.

The local `.sprintctl/sprintctl.db` remains the live source of truth. These renders are the shared reviewable view.
